(function(){'use strict';
const endpoint='https://yourfavalien-mothership.aydenmtz54.workers.dev/api/status',list=document.querySelector('.systems'),overall=document.getElementById('overall'),checked=document.getElementById('checked'),button=document.getElementById('refreshStatus');
const labels={operational:'Operational',maintenance:'Maintenance',degraded:'Service Issue',outage:'Unavailable'},escape=value=>String(value??'').replace(/[&<>"']/g,char=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]));
async function refresh(){button.disabled=true;button.textContent='CHECKING…';overall.className='overall checking';overall.innerHTML='<span class="dot"></span>Checking all systems…';list.querySelectorAll('.state').forEach(state=>{state.className='state checking';state.textContent='Checking…'});try{const response=await fetch(endpoint+'?v='+Date.now(),{cache:'no-store'});if(!response.ok)throw new Error('Status service unavailable');const data=await response.json();list.innerHTML=data.services.map(service=>'<div class="system"><div><b>'+escape(service.name)+'</b><small>'+escape(service.description)+'</small></div><span class="state '+escape(service.state)+'">'+escape(labels[service.state]||'Unknown')+(Number.isFinite(service.responseMs)?'<small>'+service.responseMs+' ms</small>':'')+'</span></div>').join('');const overallText={operational:'All systems operational',maintenance:'Scheduled maintenance in progress',degraded:'Some systems are experiencing issues',outage:'A service is currently unavailable'}[data.overall]||'Status unavailable';overall.className='overall '+data.overall;overall.innerHTML='<span class="dot"></span>'+overallText+(data.maintenance&&data.maintenanceMessage?'<small>'+escape(data.maintenanceMessage)+'</small>':'');checked.textContent='Last checked '+new Date(data.checkedAt).toLocaleString()}catch(error){overall.className='overall outage';overall.innerHTML='<span class="dot"></span>Status check unavailable';checked.textContent='We could not refresh the status. Please try again.'}finally{button.disabled=false;button.textContent='REFRESH STATUS'}}
button.addEventListener('click',refresh);document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')refresh()});window.addEventListener('yfa-status-ready',refresh,{once:true});refresh();setInterval(refresh,60000);
})();
